<?php

describe('an xit block', function() {
  xit('renders the label yellow', function() {
    expect(true)->to->beFalse(); // This shouldn't run
  });

  xit('adds (pending) to the end of the label', function() {
    expect(true)->to->beFalse(); // This shouldn't run
  });
});
